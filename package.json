{
  "name": "bios-tec-website",
  "version": "1.0.0",
  "private": true,
  "description": "bios-tec — premium IT landing page with interactive neural-network hero",
  "scripts": {
    "build": "esbuild js/hero.src.js --bundle --format=esm --minify --outfile=js/hero.js",
    "watch": "esbuild js/hero.src.js --bundle --format=esm --sourcemap --outfile=js/hero.js --watch",
    "dev": "python3 -m http.server 8099"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "lenis": "^1.1.14",
    "three": "^0.160.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
